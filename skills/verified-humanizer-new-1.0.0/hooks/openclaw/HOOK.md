# OpenClaw hook behavior

This hook adds a lightweight bootstrap reminder for Verified Humanizer.

It should remind the agent to:
- keep rewriting local
- measure before/after changes
- avoid sending raw text to external verification
- use verification only for structured metrics when needed
